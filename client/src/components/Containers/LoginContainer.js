import { StyledContainer } from "./style";

const LoginContainer = ({ children }) => {
    return (
        <StyledContainer>
            {children}
        </StyledContainer>
    );
}

export default LoginContainer;